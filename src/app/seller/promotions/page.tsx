'use client'

import { useEffect, useState } from 'react'
import { Calendar, Percent, Sparkles } from 'lucide-react'
import { SellerSidebar } from '@/components/SellerSidebar'
import { Card } from '@/components/ui/Card'
import { Button } from '@/components/ui/Button'
import { Input } from '@/components/ui/Input'
import { api } from '@/lib/api'

export default function SellerPromotionsPage() {
  const [products, setProducts] = useState<any[]>([])
  const [promotions, setPromotions] = useState<any[]>([])
  const [form, setForm] = useState({ name: '', type: 'CLEARANCE', discountType: 'PERCENTAGE', discountValue: 20, productIds: [] as string[], startsAt: new Date().toISOString().slice(0, 16), endsAt: '', status: 'DRAFT' })
  const load = async () => { const [productsResponse, promotionsResponse] = await Promise.all([api.get<any>('/seller/products?limit=100'), api.getSellerProductPromotions()]); if (productsResponse.success) setProducts(productsResponse.data?.products || []); if (promotionsResponse.success) setPromotions(promotionsResponse.data || []) }
  useEffect(() => { load() }, [])
  const create = async (event: React.FormEvent) => { event.preventDefault(); const response = await api.createSellerProductPromotion({ ...form, startsAt: new Date(form.startsAt).toISOString(), endsAt: new Date(form.endsAt).toISOString() }); if (response.success) { setForm({ ...form, name: '', productIds: [], discountValue: 20 }); await load() } else window.alert(response.error || 'Could not create promotion') }
  return <SellerSidebar><div className="space-y-6"><div><h1 className="font-display text-2xl font-bold text-warm-900">Clearance &amp; Promotions</h1><p className="text-sm text-warm-800/60">Discount slow-moving stock without changing the original price history.</p></div><div className="grid gap-6 lg:grid-cols-[360px_minmax(0,1fr)]"><Card className="p-5"><div className="mb-4 flex items-center gap-2"><Sparkles className="text-primary" size={20} /><h2 className="font-semibold text-warm-900">New promotion</h2></div><form onSubmit={create} className="space-y-3"><Input label="Promotion name" value={form.name} onValueChange={value => setForm({ ...form, name: value })} placeholder="Clearance Sale" required /><label className="block text-sm font-medium text-warm-800">Type<select value={form.type} onChange={event => setForm({ ...form, type: event.target.value })} className="mt-1 w-full rounded-xl border border-warm-200 bg-transparent p-3"><option>CLEARANCE</option><option>FLASH_SALE</option><option>BLACK_FRIDAY</option><option>END_OF_STOCK</option><option>OLD_STOCK</option></select></label><label className="block text-sm font-medium text-warm-800">Products<select multiple value={form.productIds} onChange={event => setForm({ ...form, productIds: Array.from(event.target.selectedOptions, option => option.value) })} className="mt-1 h-32 w-full rounded-xl border border-warm-200 bg-transparent p-2">{products.map(product => <option key={product.id} value={product.id}>{product.name} · GH₵{Number(product.price).toFixed(2)}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><label className="text-sm font-medium text-warm-800">Discount<select value={form.discountType} onChange={event => setForm({ ...form, discountType: event.target.value })} className="mt-1 w-full rounded-xl border border-warm-200 bg-transparent p-3"><option value="PERCENTAGE">%</option><option value="FIXED">GHS</option></select></label><Input label="Amount" type="number" min="0.01" value={String(form.discountValue)} onValueChange={value => setForm({ ...form, discountValue: Number(value) || 0 })} /></div><Input label="Starts" type="datetime-local" value={form.startsAt} onValueChange={value => setForm({ ...form, startsAt: value })} required /><Input label="Ends" type="datetime-local" value={form.endsAt} onValueChange={value => setForm({ ...form, endsAt: value })} required /><Button type="submit" icon={<Percent size={16} />}>Create promotion</Button></form></Card><div className="space-y-3">{promotions.map(promotion => <Card key={promotion.id} className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold text-warm-900">{promotion.name}</h2><p className="text-sm text-warm-800/60">{promotion.type} · {promotion.discountType === 'PERCENTAGE' ? `${promotion.discountValue}% off` : `GHS ${promotion.discountValue} off`} · {promotion.products?.length || 0} products</p></div><span className="rounded-full bg-primary/10 px-2 py-1 text-xs text-primary">{promotion.status}</span></div><p className="mt-3 flex items-center gap-1 text-xs text-warm-800/60"><Calendar size={13} /> Ends {new Date(promotion.endsAt).toLocaleString()}</p></Card>)}{promotions.length === 0 && <Card className="p-12 text-center text-sm text-warm-800/60">No product promotions yet.</Card>}</div></div></div></SellerSidebar>
}
